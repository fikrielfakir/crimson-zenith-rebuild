Inline CRUD boilerplate — every page reinvents the wheel
28 / 45 pages
Every management page manually defines useQuery + useMutation × 3 + queryClient.invalidateQueries + toast onSuccess/onError. That's ~40–60 lines of identical boilerplate per page. useAdminCRUD hook exists and is correct — but only 2 pages use it (Testimonials, TeamMembers).
Fix: Migrate the remaining 26 CRUD pages to useAdminCRUD. Start with the simplest ones: WorkOffersAdmin, VolunteerPostsAdmin, ExpertsAdmin, ProjectsAdmin — each has a single entity, simple form, no special logic. Estimated saving: ~1,200 lines of duplicated code. → Migrate WorkOffersAdmin ↗
CRITICAL
AdminPageShell components exist but are almost entirely unused
44 / 45 raw h1
AdminPageShell.tsx exports 7 shared components: AdminPageHeader, AdminCard, AdminSaveButton, AdminTableSkeleton, AdminFormSkeleton, AdminEmptyState, AdminPageError. Adoption: AdminPageHeader used in 2 pages, AdminEmptyState in 0 pages, AdminTableSkeleton in 0 pages, AdminSaveButton in 1 page. Every other page re-implements these patterns from scratch with inconsistent spacing, sizing, and copy.
Fix: For each new page you touch, replace the raw <h1 className="text-3xl font-bold"> with <AdminPageHeader title="..." description="..." action={...} />. Replace inline loading skeletons with <AdminTableSkeleton /> or <AdminFormSkeleton />. Replace "No items" divs with <AdminEmptyState />. No new code to write — just adopt what's there.
HIGH
Inline delete confirmation dialog — copy-pasted 14 times
14 pages
Every page that can delete a record re-declares: const [deletingId, setDeletingId] = useState<number | null>(null) then a full <AlertDialog> block with identical title/description/confirm/cancel. The only thing that varies is the entity name in the copy.
Fix: Create components/admin/ConfirmDeleteDialog.tsx — accepts open, onConfirm, onCancel, entityName props. Replace all 14 inline blocks. The deletingId state stays in the page but the JSX moves to the shared component. ~20 lines → 1 import per page.
HIGH
Inline search + filter state — 19 pages
19 pages
Each page declares const [searchTerm, setSearchTerm] = useState(''), a filter dropdown state, and a useMemo / inline .filter() — then re-renders an <Input placeholder="Search..."> with a magnifying glass icon. The markup and logic is near-identical across all 19 pages.
Fix: Add a useSearch(items, fields) hook for the filter logic. Add a <AdminSearchBar value={q} onChange={setQ} placeholder="Search..." /> component for the UI. Both are 20–30 line additions that replace ~50 lines per page.
HIGH
ImageUpload component exists but 5 pages bypass it
5 pages raw upload
components/admin/ImageUpload.tsx exists and is used by 4 pages (ExpertsAdmin, EventsManagement, ClubForm, GalleryManagement). But HeroSettings, NavbarSettings, PartnersManagement, PresidentMessageSettings, CitiesManagement each re-implement their own upload flow with raw FormData, apiFetch, and useState loading flags — inconsistent error handling, different size limits, different endpoints.
Fix: Extend ImageUpload with an optional endpoint prop (default /api/admin/cms/media) and migrate all 5 raw-upload pages to use it.
MEDIUM
Inline toast error handling — 25+ identical lines
25+ occurrences
The exact string onError: (e: Error) => toast({ title: 'Error', description: e.message, variant: 'destructive' }) appears 10 times. A slightly different variant onError: (err: Error) => appears 7 more times. These are functionally identical.
Fix: Already handled by useAdminCRUD — this duplication disappears automatically as pages are migrated to the hook. No separate fix needed.