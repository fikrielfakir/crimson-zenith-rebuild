Fix 1 — Missing CMS admin CRUD routes
⚠ Partial
✅
Testimonials: all 4 routes added to routes/api.php + controller methods in CmsAdminController
GET/POST /api/admin/cms/testimonials and PUT/DELETE /api/admin/cms/testimonials/{id} — fully wired
❌
Team members: no admin write routes added — only the old public GET /api/cms/team-members still exists
TeamMembersManagement.tsx calls POST/PUT/DELETE /api/admin/cms/team-members — all three return 404
❌
Clubs page: no admin PUT route added — ClubsPageSettings.tsx calls PUT /api/admin/cms/clubs-page — still 404
Still needed: Add team-members CRUD routes + controller methods (mirror the testimonials pattern you just added). Add PUT /api/admin/cms/clubs-page route + updateClubsPage method. → Generate the code ↗
Fix 2 — AdminSettings.tsx no longer a stub
⚠ Partial
✅
Now uses useQuery + useMutation — save button actually calls the API
✅
Contact tab: writes to PUT /api/admin/settings/contact — route exists ✓
✅
Integrations tab: sensibly replaced with read-only env-var documentation — correct design choice
❌
URL mismatch on SEO save — both the General tab and SEO tab write to PUT /api/admin/cms/seo
That route does not exist. The actual backend route is PUT /api/admin/settings/seo. Every SEO save will 404 silently.
⚠️
General tab reads from /api/cms/contact but that public endpoint does not appear in routes — needs verification
One-line fix: In AdminSettings.tsx change '/api/admin/cms/seo' → '/api/admin/settings/seo' (lines 75 and 154).
Fix 3 — useAdminCRUD shared hook
⚠ Partial
✅
Hook file exists at src/hooks/useAdminCRUD.ts — well-structured, generic, covers list/create/update/delete
✅
Correct TypeScript generics: T extends {id: number}, proper return types, loading states for each mutation
✅
Error handling: parses JSON error body and surfaces message in toast — good pattern
❌
Zero pages have been migrated to use it — TeamMembersManagement, TestimonialsManagement, PartnersManagement etc. all still have their own inline fetch boilerplate
The hook is built but unused — it provides no actual reduction in duplication yet
⚠️
Minor: hook always reads from readEndpoint (public) and writes to writeEndpoint (admin) — mismatch is intentional but undocumented; add a JSDoc comment so the next dev doesn't "fix" it
Next step: Migrate at least TestimonialsManagement and TeamMembersManagement as the first two consumers — that proves the hook works end-to-end and removes ~80 lines of boilerplate per page. → Migrate Testimonials ↗