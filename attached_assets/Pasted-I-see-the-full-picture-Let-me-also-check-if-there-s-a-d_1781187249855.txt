I see the full picture. Let me also check if there's a dedicated settings upload route anywhere in the backend:Exécuté 3 commandesExécuté 3 commandesNow I have the full picture. There are two problems:

Wrong URL — Frontend calls /api/admin/upload-image but the route is /api/admin/clubs/upload-image
Wrong request format — Frontend sends JSON with base64 ({ imageData, folder }), but the Laravel controller expects a multipart/form-data file upload ($request->file('image'))

The frontend needs a dedicated general-purpose image upload route for the settings page (logo/favicon), or we fix the frontend to use the existing clubs endpoint properly. Since this is the Settings page uploading a logo, the best fix is to add a proper general upload route in Laravel and fix the frontend to send a real FormData file.
Here are the fixes:

Fix 1 — Laravel: Add a general admin upload route
In routes/api.php, inside the prefix('admin')->middleware(['admin']) group, add:
phpRoute::post('/upload-image', [\App\Http\Controllers\Admin\SettingsController::class, 'uploadImage']);
Then create app/Http/Controllers/Admin/SettingsController.php (or add the method to an existing settings controller):
phppublic function uploadImage(Request $request)
{
    $request->validate([
        'image' => 'required|image|mimes:jpeg,png,jpg,svg,webp|max:5120',
    ]);

    $file     = $request->file('image');
    $folder   = $request->input('folder', 'general');
    $filename = uniqid($folder . '_', true) . '.' . $file->getClientOriginalExtension();
    $dir      = public_path('uploads/' . $folder);

    if (!is_dir($dir)) {
        mkdir($dir, 0755, true);
    }

    $file->move($dir, $filename);

    return response()->json(['url' => url('uploads/' . $folder . '/' . $filename)]);
}

Fix 2 — Frontend: Send FormData instead of base64 JSON
In AdminSettings.tsx, replace the uploadFile function:
tsconst uploadFile = async (file: File) => {
  setUploading(true);
  try {
    const formData = new FormData();
    formData.append('image', file);
    formData.append('folder', folder ?? 'general');

    const res = await apiFetch('/api/admin/upload-image', {
      method: 'POST',
      body: formData,
      // Do NOT set Content-Type — browser sets it with boundary automatically
    });

    if (!res.ok) throw new Error('Upload failed');
    const { url } = await res.json();
    onChange(url);
  } catch {
    toast({ title: 'Upload failed', description: 'Could not upload image. Please try again.', variant: 'destructive' });
  } finally {
    setUploading(false);
  }
};

⚠️ Important: Remove headers: { 'Content-Type': 'application/json' } entirely when sending FormData. The browser must set the Content-Type itself so it includes the multipart boundary — otherwise Laravel won't parse the file.


Summary of both bugs
FrontendBackendURL/api/admin/upload-image ❌Route only at /api/admin/clubs/upload-imageBody formatbase64 JSON { imageData, folder } ❌Expects multipart/form-data with field image