Critical — breaks right now

CRITICAL
3 admin CMS routes return 404
/api/admin/cms/testimonials, /api/admin/cms/team-members (POST/PUT/DELETE), and /api/admin/cms/clubs-page (PUT) are called by the frontend but never registered in routes/api.php.
Fix: add CRUD routes + controller methods. Testimonials fix already detailed in previous message.
Backendroutes/api.php
HIGH
Inconsistent camelCase ↔ snake_case contract
Frontend sends isApproved, isActive, logoUrl etc. Laravel receives is_approved, is_active in DB columns. Several controllers handle this manually and inconsistently — some use $request->isApproved, others $request->is_approved, risking silent null writes.
Fix: add protected $casts + a shared transformer, or enable snake_case on all API responses via a middleware/Resource class.
BackendData contract
HIGH
Dashboard fires 4 separate queries on load
AdminDashboard.tsx calls /api/admin/stats, /activity, /upcoming-events, and /charts concurrently. Any one failure shows a partial dashboard. Stats are on a separate stale timer from charts — they can desync visually.
Fix: merge into one /api/admin/dashboard endpoint, or use React Query's useQueries with a shared error boundary.
FrontendPerformance
MEDIUM
AdminSettings page is a hardcoded stub
AdminSettings.tsx has only 2 useState calls and zero API calls. The "Save" button calls toast() but saves nothing. The SEO tab and Integrations tab are completely static.
Fix: wire to /api/admin/settings and /api/admin/settings/seo (routes exist in Laravel) using useQuery + useMutation.
FrontendStub
MEDIUM
SystemMonitoring and ThemeCustomization are empty shells
SystemMonitoring.tsx renders two cards with placeholder text. ThemeCustomization.tsx renders a single card saying "coming soon". Both appear in the sidebar nav so users hit dead pages.
Fix: either implement them or hide from nav until ready. ThemeSettings.tsx (the real theme page) is complete — update the nav link to point there instead.
FrontendStub
LOW
Duplicate theme pages in App.tsx
/admin/theme-old → ThemeCustomization (stub) and /admin/theme → ThemeSettings (real). Both are registered. The sidebar nav points to /admin/theme (correct) but stale bookmarks / references to "theme-old" would confuse users.
Fix: delete the /admin/theme-old route and the ThemeCustomization.tsx file.
FrontendCleanup
LOW
Sidebar "Clubs Showcase" link routes to full CRUD management
The nav item labeled admin.nav.clubsShowcase links to /admin/customization/clubs which renders the full ClubsManagement CRUD page — the same component as /admin/clubs. Two paths, same behavior, different label = cognitive confusion.
Fix: create a real "Clubs showcase settings" page (section title, display count, sort order), or rename the nav item to "Manage Clubs" and merge with /admin/clubs.
UXNav